# Demo data

This directory is reserved for deterministic fixture extracts used only for local development. The current MVP keeps fixtures in the backend module to simplify offline startup; all API responses expose the provenance label `DEMO DATA / SYNTHETIC DATA`.
