# Data documentation

All bundled records are labelled `DEMO DATA / SYNTHETIC DATA`. Satellite-like fields are surface/environmental/geological proxies; no live imagery is bundled. Operational ingestion must validate coordinate bounds, timestamps, units, duplicates, missingness, and source authorization before data reaches a model.
